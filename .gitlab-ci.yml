image: docker:latest
services:
- docker:dind

stages:
- build

test:
  stage: build
  image: golang:1.13
  script:
    - go test -race -v ./...

verify:
  stage: build
  script:
    - apk update && apk add git
    - ./verify.sh
  except:
    - master
